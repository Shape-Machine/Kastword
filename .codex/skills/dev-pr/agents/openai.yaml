interface:
  display_name: "Create and Merge PR"
  short_description: "Create, monitor, and merge a feature PR"
  default_prompt: "Use $dev-pr to validate, open, monitor, and merge the current feature branch."
