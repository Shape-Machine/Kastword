interface:
  display_name: "Create and Merge PR"
  short_description: "Refresh screenshots, validate, and merge a PR"
  default_prompt: "Use $dev-pr to refresh screenshots, validate, open, monitor, and merge the current feature branch with my permission."
